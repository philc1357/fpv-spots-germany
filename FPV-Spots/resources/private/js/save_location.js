// map.on('click', function(e) {
//   const { lat, lng } = e.latlng;

//   L.marker([lat, lng]).addTo(map)
//     .bindPopup(`Du hast hier geklickt:<br><b>${lat.toFixed(5)}, ${lng.toFixed(5)}</b>`)
//     .openPopup();

//   // Optional: in Konsole ausgeben oder später speichern
//   console.log('Speichern:', lat, lng);
// });
